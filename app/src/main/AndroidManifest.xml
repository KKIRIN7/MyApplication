<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TrainDeltoid"></activity>
        <activity android:name=".TrainDeltoidDescription" />
        <activity android:name=".TrainCenter" />
        <activity android:name=".TrainLegs" />
        <activity android:name=".TrainArm" />
        <activity android:name=".TrainBack" />
        <activity android:name=".TrainBig" />
        <activity android:name=".TestActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ShopActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".HelpActivity"
            android:label="@string/app_name" />
    </application>

</manifest>